#include <stdio.h>

/**
 * main - determines to print single digit  numbers
 *
 * Return: 0 is for success
 */
int main(void)
{

	int i = 0;

	while (i < 10)
	{
		printf("%d", i);
		i++;
	}
	return (0);
}
